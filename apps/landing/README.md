# Website

Rocky's static landing page. English is served from `/` and Brazilian Portuguese from `/pt-BR/`.

Preview it locally with:

```shell
python3 -m http.server 4173 --directory apps/landing
```

This is the previous landing, kept only as a reference. The `Site` workflow now builds and publishes the Next.js landing in `apps/web`.
